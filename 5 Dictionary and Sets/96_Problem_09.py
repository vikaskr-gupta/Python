S = {8, 7, 12, "Harry", [1, 2]}
print(S)

# We can not store a List in the Set
# Set is Hashable(Immutable(Unchangable)) but List is Unhashable(Mutable(Changable))
# Hence Question is Wrong

# Rule: All immutable objects are hashable but not all hashable objects are immutable
